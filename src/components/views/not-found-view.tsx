import MessageView from "@/components/views/message-view";

export default function NotFoundView() {
  return <MessageView title={"404"} message={"The page that you are looking for does not exist!"} />;
}
